exports.sendLockoutEmail = async (email, name) => {
    // Just log for now. Replace with real email logic (e.g., nodemailer)
    console.log(`📧 Sending lockout alert to ${email} for ${name}`);
  };
  